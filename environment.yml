name: env_govlatinamerica
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - tinydb
  - beautifulsoup4
  - yattag
  - python-dotenv
  - lxml
  - pip
  - pip:
    - wget
  
  
prefix: ~/.conda/envs/env_govlatinamerica
